return {
  {
    -- NOTE(miha): This disables file tabs on the top (like other editors that have files as tabs on top).
    "akinsho/bufferline.nvim",
    enabled = false,
  },
  {
    -- NOTE(miha): This disables automatically closing pairs (i.e. (,",[,...)
    "echasnovski/mini.pairs",
    enabled = false,
  },
}
